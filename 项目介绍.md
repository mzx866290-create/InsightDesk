# 企业专属 AI 知识库系统

> 基于 LangChain + RAG + MCP 协议构建的轻量级企业知识库，支持本地部署与云端模型双模式，让企业内部知识真正"活"起来。

---

## 项目定位

企业日常运营中积累了大量文档、政策、流程手册，但这些知识往往"沉睡"在文件夹里，难以快速检索和利用。本项目通过 RAG（检索增强生成）技术，将企业文档向量化存储，结合大语言模型实现自然语言问答，同时支持联网搜索补充外部信息，构建一套完整的企业智能知识助手。

---

## 技术架构

```
┌─────────────────────────────────────────────────────┐
│                    前端交互层                         │
│                   Streamlit UI                       │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│              LangChain Agent 编排层                  │
│   工具路由 │ RAG Chain │ 意图识别 │ 对话管理          │
└──────┬─────────────────────────┬────────────────────┘
       │                         │
┌──────▼──────────┐   ┌──────────▼──────────────────┐
│   FAISS 向量库   │   │       工具层                 │
│                 │   │  query_knowledge (内部检索)   │
│  企业文档向量化  │   │  web_search (Tavily 联网)    │
│  bge-large-zh   │   │  quick_answer (快速问答)     │
└─────────────────┘   └──────────────────────────────┘
                                 │
┌────────────────────────────────▼────────────────────┐
│                   模型层                             │
│   本地: Ollama (Qwen2.5 / Llama 等)                 │
│   云端: OpenRouter / OpenAI 兼容接口                 │
└─────────────────────────────────────────────────────┘
```

### 核心模块说明

| 模块 | 文件 | 职责 |
|------|------|------|
| 前端界面 | `app.py` | Streamlit UI，文档上传，聊天交互，模型配置 |
| Agent 编排 | `agent_core.py` | LangChain Agent，工具注册，模型工厂，意图路由 |
| 文档管道 | `doc_pipeline.py` | 文档解析、分块、向量化、FAISS 存储 |
| 知识库服务 | `mcp_servers/knowledge_server.py` | MCP 协议知识库服务 |
| 搜索服务 | `mcp_servers/search_server.py` | MCP 协议联网搜索服务 |

---

## 核心功能

### 1. RAG 语义检索
- 将企业文档（PDF、Word、Markdown、CSV、TXT）解析并切块
- 使用 `bge-large-zh-v1.5` 中文 embedding 模型向量化
- 基于 FAISS 向量数据库进行语义相似度检索
- 检索结果附带来源文件信息，支持溯源

### 2. 智能工具路由
Agent 遵循优先级策略自动选择工具：
- **优先**：`query_knowledge` 检索企业内部知识库
- **补充**：`web_search` 通过 Tavily API 获取实时外部信息
- **快速**：`quick_answer` 直接返回 AI 总结答案
- 回答时自动标注信息来源（内部文档 / 网络搜索）

### 3. 双模型后端
| 模式 | 适用场景 | 配置 |
|------|---------|------|
| 本地 (Ollama) | 内网环境，数据安全要求高 | 无需 API Key，支持 Qwen2.5、Llama 等 |
| 云端 (OpenRouter) | 快速体验，无本地 GPU | 需要 API Key，支持 GPT-4、Claude 等 |

运行时可在 Streamlit 侧边栏实时切换模型，无需重启服务。

### 4. 文档管理
- 支持多文件批量上传
- 自动识别文件格式并调用对应解析器
- 向量化完成后立即可用于检索
- 提供知识库统计（文档片段数、存储路径）

---

## 技术亮点

**MCP 协议架构**
工具层基于 MCP（Model Context Protocol）协议设计，知识库检索和联网搜索作为独立 MCP Server 运行，Agent 通过标准协议调用。这使得新工具的接入只需新增一个 MCP Server 文件并注册，无需修改核心逻辑。

**数据安全**
Ollama 本地模式下，所有数据（文档、对话、向量）均在本地处理，不经过任何外部网络，满足企业内网安全要求。

**异步架构**
Agent 调用链全程异步（`asyncio`），Streamlit 通过独立线程运行异步代码，避免事件循环冲突，保证 UI 响应流畅。

**中文优化**
- Embedding 模型选用 `BAAI/bge-large-zh-v1.5`，专为中文语义检索优化
- 文档分块策略针对中文标点（`。`、`；`）进行优化
- 系统提示词强制中文优先回答

---

## 部署方案

### 方案 A：本地 Ollama（推荐内网环境）

```env
LLM_PROVIDER=ollama
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_BASE_URL=http://localhost:11434
TAVILY_API_KEY=your_key  # 可选，用于联网搜索
```

**前置条件**：安装 Ollama 并拉取模型
```bash
ollama pull qwen2.5:7b
```

### 方案 B：云端 OpenRouter（推荐快速体验）

```env
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=your_key
OPENROUTER_MODEL=qwen/qwen-2.5-72b-instruct
```

### 启动

```bash
pip install -r requirements.txt
streamlit run app.py
```

访问 `http://localhost:8501`

---

## 使用场景

- **企业政策问答**：上传 HR 手册、规章制度，员工直接提问
- **技术文档检索**：上传 API 文档、架构设计，开发者快速查阅
- **项目知识沉淀**：上传会议纪要、项目文档，新成员快速上手
- **混合信息查询**：内部知识 + 实时网络信息联合回答

---

## 技术栈

| 类别 | 技术 |
|------|------|
| Agent 框架 | LangChain 0.3+ |
| 本地模型 | Ollama + Qwen2.5 |
| 云端模型 | OpenRouter（OpenAI 兼容） |
| 向量数据库 | FAISS |
| Embedding | BAAI/bge-large-zh-v1.5 |
| 工具协议 | MCP Python SDK |
| 联网搜索 | Tavily API |
| 文档解析 | pypdf, docx2txt, unstructured |
| 前端 | Streamlit |
| HTTP 客户端 | httpx |

---

## 迭代规划

| 阶段 | 目标 | 状态 |
|------|------|------|
| P0 - MVP | RAG 问答 + 文档上传 + 双模型支持 | ✅ 已完成 |
| P1 - 功能增强 | 多轮对话记忆、引用溯源、文档管理 UI | 🔄 规划中 |
| P2 - 体验优化 | 用户权限系统、审计日志 | 📋 待开发 |
| P3 - 生产部署 | Docker 容器化、监控告警、性能优化 | 📋 待开发 |

---

## 项目结构

```
AI智能体/
├── app.py                      # Streamlit 前端入口
├── agent_core.py               # LangChain Agent 编排核心
├── doc_pipeline.py             # 文档处理与向量化管道
├── mcp_servers/
│   ├── knowledge_server.py     # 知识库 MCP Server
│   └── search_server.py        # 联网搜索 MCP Server
├── vector_store/               # FAISS 向量库（自动生成）
├── models/                     # 本地 Embedding 模型缓存
├── requirements.txt            # Python 依赖
├── .env.example                # 环境变量模板
├── setup.bat                   # Windows 一键安装脚本
└── start.bat                   # Windows 一键启动脚本
