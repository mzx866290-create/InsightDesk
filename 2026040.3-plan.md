# 项目现状审计与下阶段计划

> 实施进度（2026-04-03）：
> - Phase A 已完成：`api_server.py` 重复定义清理、并行 SSE 结束逻辑修复、RAG 参数调整、`auto` 模式自适应修复
> - Phase B 已完成：`项目介绍.md` 与 `plan.md` 已同步到当前实现状态
> - Phase C 仍待架构决策；Phase D / Phase E 尚未开始

**日期**: 2026-04-03  
**审计范围**: 全量代码（后端 Python + 前端 React/TypeScript）

---

## 一、项目当前整体状态

### 技术架构（实际）

```
用户浏览器
    └── React/TypeScript 前端 (Vite + Tailwind)
              │ SSE + REST
    └── FastAPI 后端 (api_server.py)
              │
    ├── Agent 编排 (agent_core.py)
    │     ├── LangGraph 轻量 Agent（默认 auto 模式固定使用）
    │     └── Function Calling Agent（需手动指定）
    │
    ├── 文档管道 (doc_pipeline.py)
    │     ├── FAISS 向量库（粗排）
    │     └── CrossEncoder 重排（精排）
    │
    ├── DeckSpec 演示稿生成 (deck_service.py)
    │     ├── SourcePack 构建
    │     ├── Outline 规划（LLM）
    │     ├── 内容草稿生成（LLM）
    │     └── python-pptx 导出
    │
    └── 聊天存储 (chat_store.py) — SQLite 持久化
```

### 已完成功能（实际代码层面）

| 功能模块 | 状态 | 说明 |
|---------|------|------|
| RAG 检索（FAISS + CrossEncoder 二段重排） | ✅ 完成 | fetch_k=6, top_k=2 |
| 多轮对话记忆（SQLite 持久化） | ✅ 完成 | 比 plan.md 描述的 InMemory 更好 |
| LangGraph 轻量 Agent | ✅ 完成 | 3 节点 StateGraph |
| Function Calling Agent | ✅ 完成 | 需手动指定 |
| FastAPI REST + SSE 接口 | ✅ 完成 | 含多模型并行 |
| React 多面板聊天界面 | ✅ 完成 | 最多 6 个面板并行 |
| DeckSpec 结构化演示稿中间层 | ✅ 完成 | 含 evidence 引用体系 |
| Deck 编辑器（三栏布局） | ✅ 完成 | 含质量状态标记 |
| 仪表盘卡片（Dashboard Card） | ✅ 完成 | ECharts 渲染 |
| 系统提示词管理（多角色） | ✅ 完成 | 含知识库绑定 |
| 文件附件解析（对话中） | ✅ 完成 | 支持 PDF/Word/CSV 等 |
| 工具集（知识库+联网+网页抓取） | ✅ 完成 | 6 个工具 |

---

## 二、发现的问题清单

### 🔴 高优先级：代码 Bug

#### Bug-01：`api_server.py` 中函数重复定义（直接覆盖）

```python
# 第 238 行（旧版，2个参数）—— 被后面的覆盖，成为死代码
def _validate_chat_payload(message: str, images: list[ImageInput]) -> None:
    ...

# 第 296 行（新版，3个参数）—— 实际生效
def _validate_chat_payload(message: str, images: list[ImageInput], files: list[FileInput]) -> None:
    ...
```

同样问题出现在 `_build_user_input` 函数（第 260 行和第 318 行各定义一次）。旧版本成为死代码，但会让代码阅读者混乱。

**影响**: 代码可读性差，潜在维护风险。

#### Bug-02：`list_knowledge_bases` 重复导入 4 次

```python
@app.get("/api/knowledge-bases")
async def list_knowledge_bases():
    from doc_pipeline import DocPipeline
    from doc_pipeline import DocPipeline  # 重复
    from doc_pipeline import DocPipeline  # 重复
    from doc_pipeline import DocPipeline  # 重复
```

**影响**: 代码质量问题，应清理。

#### Bug-03：并行 SSE 流合并逻辑竞态风险

`chat_parallel` 的 `event_generator` 中 `pending` 使用 `nonlocal` 被多个协程并发修改，且终止逻辑依赖 `finished_producers` + `all_done` 的组合判断，逻辑复杂、容易出现过早退出或永久挂起。

**影响**: 多模型并行对话可能出现某面板提前断流。

---

### 🟡 中优先级：架构遗留问题

#### Arch-01：双前端并存（Streamlit + React）

- `app.py`（Streamlit 前端）仍然存在且 `requirements.txt` 保留 `streamlit>=1.35.0`
- 项目已以 React + FastAPI 为主架构
- `app.py` 是否仍有人使用、是否保持功能同步：不清楚

**建议**: 明确 `app.py` 定位（保留为备用只读模式 or 彻底废弃），从 `requirements.txt` 移除 `streamlit` 或单独维护一个 `requirements-streamlit.txt`。

#### Arch-02：MCP 服务器文件未集成

- `mcp_servers/knowledge_server.py` 和 `search_server.py` 存在
- 实际 Agent 工具链在 `agent_core.py` 中以 LangChain `@tool` 形式直接定义，**未通过 MCP 协议调用**
- MCP 文件成为废弃/未集成代码

**建议**: 要么集成 MCP 协议（将工具迁移为 MCP Server + `langchain-mcp-adapters` 调用），要么删除该目录并在文档中说明。

#### Arch-03：`auto` 模式未体现双 Agent 自适应

项目介绍中描述"本地模型→LangGraph，云端模型→Function Calling"，实际代码：
```python
if agent_mode == "auto":
    actual_mode = "langgraph"  # 无论本地还是云端，一律 LangGraph
```

**影响**: 云端大模型（GPT-4、Claude）无法通过 auto 模式自动切换到 Function Calling，丧失原生工具调用能力。

---

### 🟡 中优先级：参数配置偏差

#### Param-01：知识库检索参数偏保守

- 实际配置：`DEFAULT_KB_TOP_K = 2`，`fetch_k = 6`
- `plan.md` 描述：Top 3，`fetch_k = 10`
- `项目介绍.md` 描述：粗排 fetch_k=10 → 精排 Top 3

两个关键参数均低于设计值，会影响检索召回率。

---

### 🟢 低优先级：文档与代码不同步

#### Doc-01：`项目介绍.md` 仍以 Streamlit 为主描述

- 部署方式、启动命令、UI 功能说明均指向 Streamlit
- 技术架构图中前端节点为 `Streamlit UI (app.py)`

#### Doc-02：`项目介绍.md` 迭代规划未更新

| 规划阶段 | 文档状态 | 实际状态 |
|---------|---------|---------|
| P0 - MVP | ✅ 已完成 | ✅ |
| P1 - 功能增强 | ✅ 已完成 | ✅ |
| P2 - 质量提升 | 🔄 规划中 | ✅ 已完成（混合检索除外） |
| P3 - 体验优化 | 📋 待开发 | 部分完成（多角色系统已实现）|

#### Doc-03：`plan.md` 二期目标已全部实现但状态未更新

Rerank 二段重排、多轮对话记忆均已实现并超预期（SQLite 持久化 > InMemory 设计）。

---

### 🔵 功能缺口（PPT_REDESIGN_PLAN.md 中提出但未实现）

| 功能 | 状态 | 说明 |
|------|------|------|
| DeckSpec 结构化中间层 | ✅ 已实现 | `deck_service.py` |
| 三栏 Deck 编辑器 | ✅ 已实现 | `DeckEditorModal.tsx` |
| 单页重生成 API | ❌ 未实现 | 无 `/slides/{id}/regenerate` 端点 |
| Node/PptxGenJS 渲染服务 | ❌ 未实现 | 导出仍用 `python-pptx` |
| Playwright PDF 导出 | ❌ 未实现 | — |
| 多主题模板系统 | ❌ 未实现 | 单一默认布局 |
| 来源选择 UI（勾选资料入稿） | ❌ 未实现 | — |
| Web 分享链接 | ❌ 未实现 | — |

---

## 三、下阶段工作计划（优先级排序）

### Phase A：清理技术债（1-2天）

1. **清理 `api_server.py` 重复定义**
   - 删除 `_validate_chat_payload` 和 `_build_user_input` 的旧版定义（2 参数版）
   - 清理 `list_knowledge_bases` 中重复 import

2. **修复并行 SSE 终止逻辑**
   - 使用更健壮的 `asyncio.Event` 或计数器替代 `nonlocal pending`

3. **修正 RAG 检索参数**
   - `DEFAULT_KB_TOP_K = 3`（从 2 改为 3）
   - 知识库工具调用 `fetch_k = 10`（从 6 改为 10）

4. **修复 `auto` 模式 Agent 自适应逻辑**
   - `provider == "cloud"` → 默认 `function_calling`
   - `provider == "local"` → 默认 `langgraph`

### Phase B：文档同步（0.5天）

1. **更新 `项目介绍.md`**
   - 架构图替换为 React + FastAPI 实际架构
   - 更新部署方式、启动命令
   - 更新迭代规划状态
   - 更新技术栈说明

2. **归档 `plan.md`**
   - 标注二期目标已全部完成

### Phase C：架构澄清（讨论决策）

1. **明确 `app.py` 定位**
   - 方案 A：完全废弃，删除，移除 streamlit 依赖
   - 方案 B：保留为轻量备用模式，与 React 并行维护

2. **明确 MCP 集成策略**
   - 方案 A：集成 MCP，将工具迁移为标准 MCP Server
   - 方案 B：清理废弃文件，工具保持 LangChain `@tool` 形式

### Phase D：PPT 功能完善（1-2周）

按 `PPT_REDESIGN_PLAN.md` Phase 2-3 推进：

1. **单页重生成 API**
   - 后端：`POST /api/decks/{deck_id}/slides/{slide_id}/regenerate`
   - 前端：右侧面板新增"重生成此页"按钮

2. **改善 PPTX 导出质量**
   - 演讲备注写入 PPTX 备注区
   - 附录页来源清单格式化

3. **主题模板（v1）**
   - 定义 2-3 套基础 layout（商务蓝、极简白、深色）
   - 导出时根据模板选择配色和字体

### Phase E：测试补全（持续）

1. 为核心模块补充单元测试：
   - `doc_pipeline.py`：向量化、重排、检索
   - `deck_service.py`：SourcePack 构建、DeckSpec 生成
   - `api_server.py`：关键端点的集成测试

2. 将根目录散落的测试文件统一迁移到 `tests/` 目录

---

## 四、当前代码热点文件风险评估

| 文件 | 行数 | 风险等级 | 主要风险 |
|------|------|---------|---------|
| `api_server.py` | ~900 | 🔴 高 | 重复定义、并发逻辑、单文件过大 |
| `agent_core.py` | ~900 | 🟡 中 | auto 模式逻辑错误、超时处理复杂 |
| `deck_service.py` | ~700 | 🟡 中 | LLM 生成稳定性依赖 fallback 链 |
| `doc_pipeline.py` | ~400 | 🟢 低 | 相对稳定 |
| `chat_store.py` | ~300 | 🟢 低 | 相对稳定 |
| `frontend/src/stores/chatStore.ts` | ~200 | 🟢 低 | 持久化分区设计合理 |
| `frontend/src/components/reports/DeckEditorModal.tsx` | ~400 | 🟡 中 | 缺少单页重生成功能 |

---

## 五、总结

项目整体进展良好，核心 RAG + Agent + React 前端已形成完整闭环，DeckSpec 演示稿架构设计超出原始规划水平。主要欠缺在于：

1. 代码层面存在可见的重复定义 bug，需尽快清理
2. 文档严重滞后于代码实现，对外展示/团队协作有影响
3. `auto` Agent 模式的自适应逻辑未落地，云端模型无法发挥工具调用原生优势
4. PPT 功能的渲染层和高级交互功能尚有缺口

**近期优先级**：Phase A（代码清理）> Phase B（文档同步）> Phase D（PPT 功能完善）
